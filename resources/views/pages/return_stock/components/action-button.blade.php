<button type="button"
        class="btn btn-danger btn-sm action-btn delete-btn"
        data-id="{{ $returnStock->id }}"
        data-inventory-id="{{ $returnStock->inventory_stock_id }}"
        data-qty="{{ $returnStock->qty }}"
        data-other-income-id="{{ $returnStock->other_income_id }}">
    <i class="fa fa-trash-alt"></i>
</button>
